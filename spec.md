# Specification

## Summary
**Goal:** Fix the backend seed data so the Ferrari Cheque fabric product (with updated minimum order quantity and colour values) is correctly stored and returned by the API, making it visible on the frontend.

**Planned changes:**
- Reseed the backend with the correct Ferrari Cheque fabric product data, including the updated minimum order quantity and colour values
- Ensure the `getProducts` endpoint returns the Ferrari Cheque product after reseeding

**User-visible outcome:** The Ferrari Cheque fabric product appears correctly on both the Products listing page and the Home page featured products grid.
